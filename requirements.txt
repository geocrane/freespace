# Зависимости веб-версии — основного интерфейса.
#   pip install -r requirements.txt
#
# Ядро (freespace/core) работает на голой стандартной библиотеке, поэтому
# для одного лишь спайка можно не ставить и это: он умеет подниматься на
# stdlib-сервере, см. `python freespace/web/spike.py --stdlib`.
#
# Qt-версия заморожена и ставится отдельно:  pip install "PySide6>=6.6"

fastapi>=0.110
uvicorn>=0.27
